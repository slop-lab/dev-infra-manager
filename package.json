{
  "name": "@dim/workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "pnpm --filter @dim/manager run cli --",
    "codex-workspace": "pnpm --filter @dim/codex-workspace run cli",
    "workspace:check": "pnpm --recursive run check",
    "workspace:test": "pnpm --recursive run test",
    "workspace:build": "pnpm --recursive run build"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.13.1"
}
