{
  "name": "@slop-lab/dev-infra-manager-workspace",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "pnpm --filter @slop-lab/dev-infra-manager-core run build && pnpm --filter @slop-lab/dim-cli run cli",
    "workspace:check": "pnpm --recursive run check",
    "workspace:test": "pnpm --recursive run test",
    "workspace:build": "pnpm --recursive run build"
  },
  "engines": {
    "node": "^24.0.0 || ^26.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.13.1"
}
