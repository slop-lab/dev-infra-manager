services:
  caddy:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "100.64.0.10:80:80"
      - "100.64.0.10:443:443"
      - "100.64.0.10:443:443/udp"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      CF_API_TOKEN: ${CF_API_TOKEN:?set CF_API_TOKEN}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config

volumes:
  caddy-data:
  caddy-config:
