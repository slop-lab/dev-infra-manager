name: QEMU release gate

on:
  workflow_dispatch:
    inputs:
      root-ref:
        description: Exact root branch, tag, or commit to combine with this development ref
        type: string
        required: true
        default: main

permissions:
  contents: read

jobs:
  qemu-runc:
    uses: dim-dim/verification/.gitea/workflows/repository-set.yml@e7c56a17033a677627d94107e2abf79c9a43ba89
    with:
      candidate-repository: development
      candidate-ref: ${{ gitea.sha }}
      development-ref: ${{ gitea.sha }}
      root-ref: ${{ inputs.root-ref }}
      gate: kvm
      runner-label: dim-qemu
      backend: runc

  qemu-sysbox:
    uses: dim-dim/verification/.gitea/workflows/repository-set.yml@e7c56a17033a677627d94107e2abf79c9a43ba89
    with:
      candidate-repository: development
      candidate-ref: ${{ gitea.sha }}
      development-ref: ${{ gitea.sha }}
      root-ref: ${{ inputs.root-ref }}
      gate: kvm
      runner-label: dim-qemu
      backend: sysbox

  qemu-gvisor:
    uses: dim-dim/verification/.gitea/workflows/repository-set.yml@e7c56a17033a677627d94107e2abf79c9a43ba89
    with:
      candidate-repository: development
      candidate-ref: ${{ gitea.sha }}
      development-ref: ${{ gitea.sha }}
      root-ref: ${{ inputs.root-ref }}
      gate: kvm
      runner-label: dim-qemu
      backend: gvisor

  qemu-rootless-podman:
    uses: dim-dim/verification/.gitea/workflows/repository-set.yml@e7c56a17033a677627d94107e2abf79c9a43ba89
    with:
      candidate-repository: development
      candidate-ref: ${{ gitea.sha }}
      development-ref: ${{ gitea.sha }}
      root-ref: ${{ inputs.root-ref }}
      gate: kvm
      runner-label: dim-qemu
      backend: rootless-podman
