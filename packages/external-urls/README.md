# DIM External URLs Plugin

This local-build DIM plugin registers a Host-based reverse proxy route provider
and independent Tailscale and Cloudflare URL providers. See
[`docs/external-urls.md`](../../docs/external-urls.md) for the controller,
deployment, DNS, tunnel, and workspace API contracts.
